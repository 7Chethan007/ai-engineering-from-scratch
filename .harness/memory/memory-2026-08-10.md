# Memory — 2026-08-10

## Colab GPU environment (for the curriculum)
- Verified working setup: Google Colab, hosted runtime, T4 GPU.
- To get CUDA torch: `!pip install torch --index-url https://download.pytorch.org/whl/cu121`
- Always verify with `torch.cuda.is_available()` after `Runtime > Restart session`.
- Gotcha: a local runtime (own machine) never has the GPU, and runtime-type changes don't apply until the session restarts.
- Note: Colab sessions are ephemeral — the CUDA torch install must be re-run after the runtime recycles (or add it as the first cell).

## Git credentials (two GitHub accounts on this Mac)
- Accounts: `7Chethan007` (repo owner) and `7Chethan-007`.
- osxkeychain caches one HTTPS credential per host and `gh-switch` does not update it.
- If push 403s as the wrong account: erase the `github.com` keychain entry, then store the active account's token from `gh auth token`.
