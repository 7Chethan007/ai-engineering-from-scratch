# LinkedIn Post — 2026-08-10

## Draft
> **Starting my "AI from Scratch" build — Phase 00: Setup & Tooling.**
>
> Not a course. A 503-lesson build-everything-by-hand curriculum. No frameworks until I've written the math myself.
>
> Day 1: got a real GPU (Colab T4), wrote a 5000×5000 matmul benchmark, and hit the bug that teaches you GPU fundamentals fast — async execution. My timer said 0.000s. Not because the GPU is magic; because Python submits the job and walks away. `torch.cuda.synchronize()` forces it to wait.
>
> Following the 503-lesson roadmap (link below). Committing every lesson to GitHub as I go — benchmark notebook is live.
>
> #chethans-ai-learning-journey #AIFromScratch #DeepLearning #GPU #LearningInPublic

## Copy-Paste Ready (post this exactly)
> **Starting my "AI from Scratch" build — Phase 00: Setup & Tooling.**
>
> Not a course. A 503-lesson build-everything-by-hand curriculum. No frameworks until I've written the math myself.
>
> Day 1: got a real GPU (Colab T4), wrote a 5000×5000 matmul benchmark, and hit the bug that teaches you GPU fundamentals fast — async execution. My timer said 0.000s. Not because the GPU is magic; because Python submits the job and walks away. `torch.cuda.synchronize()` forces it to wait.
>
> Following the full 503-lesson roadmap here:
> https://github.com/7Chethan007/ai-engineering-from-scratch/blob/main/ROADMAP.md
>
> Committing every lesson to GitHub as I go — my fork is live here:
> https://github.com/7Chethan007/ai-engineering-from-scratch
>
> #chethans-ai-learning-journey #AIFromScratch #DeepLearning #GPU #LearningInPublic

## Images (attach to post, in this order)
1. `../images/gpu-no-synchronize.png` — benchmark output with the timing bug: GPU shows ~0.000s because `torch.cuda.synchronize()` is missing.
2. `../images/gpu-synchronize.png` — same benchmark after the fix: real GPU time + speedup printed.

(Verify these two labels match what the screenshots actually show before posting — the model editing this file could not view the images.)

## Status
- [ ] Posted
- Posted URL:

## Engagement notes (optional, fill in later)
- GitHub PR #1 (`my-progress` → `main`) merged — harness + instruction files are now on the fork: https://github.com/7Chethan007/ai-engineering-from-scratch/pull/1
